# CRAN fix: drop CXX_STD = CXX11; modern R defaults to C++17 and CRAN flags explicit C++11 as unnecessary
# CRAN fix: do not set CXX here; the compiler is R's to choose (R-exts 1.2.1),
# and hardcoding one breaks on machines where it is not installed
PKG_CPPFLAGS= -I../inst/include/ -I./inst/ -DHAVE_CONFIG_H -DARMA_USE_CXX11 -I./sundials/sundials @SUNDIALR_CPPFLAGS@
PKG_LDFLAGS = ../inst/lib
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) -L$(PKG_LDFLAGS) -L../inst/lib @SUNDIALR_LIBS@
LIBS=-L./ -L../inst/lib
